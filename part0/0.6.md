```mermaid
sequenceDiagram
    participant user
    participant browser
    participant server

   user->>browser: "Exercise 0.6"  in the text field
    activate user
    user->>browser: "Save" button
    deactivate user
    Note right of browser: Browser sends the input to the server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: HTTP 201 Created - {"message":"note created"}
    deactivate server

    browser->>browser: Render new note
```
